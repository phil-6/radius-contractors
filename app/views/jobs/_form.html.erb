<%= form_with(model: [@contractor, job]) do |form| %>
  <% if job.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-8">
      <div class="form-floating">
        <%= form.textarea :description, placeholder: "Job Description", class: "form-control", required: true %>
        <%= form.label :description, class: "form-label" %>
      </div>
    </div>
    <div class="col-4">
      <p>What was the work that <%= @contractor.name %> did?</p>
    </div>
  </div>

  <div class="row">
    <div class="col-8">
      <div class="form-floating">
        <%= form.select :state, options_for_select(["Quote", "Planned", "In Progress", "Complete"]),
                        { prompt: "Select State" }, { class: "form-select", required: true } %>
        <%= form.label :state, class: "form-label" %>
      </div>
    </div>
    <div class="col-4">
      <p>What state is the job in? E.G. "Quote", "Planned", "In Progress", "Complete"</p>
    </div>
  </div>

  <div class="row">
    <div class="col-4">
      <%= form.label :start_date, class: "form-label" %>
      <%= form.date_field :start_date, class: "form_control" %>
    </div>

    <div class="col-4">
      <%= form.label :end_date, class: "form-label" %>
      <%= form.date_field :end_date, class: "form_control" %>
    </div>
  </div>

  <div class="row">
    <div class="col-8">
      <div class="form-floating">
        <%= form.textarea :review, placeholder: "Review", class: "form-control" %>
        <%= form.label :review, class: "form-label" %>
      </div>
    </div>
    <div class="col-4">
      <p>What did you think of the work that <%= @contractor.name %> did?</p>
    </div>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
