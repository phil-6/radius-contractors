<% content_for :title, "New job" %>
<main class="container">
  <div class="card form-card">
    <h1>What did <%= @contractor.name %> do for you?</h1>

    <%= render "form", job: @job %>
  </div>

  <hr>

  <div>
    <%= link_to "Back to contractor", contractor_path(@contractor) %>
  </div>
</main>
