<!DOCTYPE html>
<html lang="en-GB" data-bs-theme="dark">
<head>
  <title><%= content_for(:title) || "Radius Contractors" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <%= favicon_link_tag asset_path('favicon/favicon.ico') %>
  <%= favicon_link_tag asset_path('favicon/android-chrome-192x192.png') %>
  <%= favicon_link_tag asset_path('favicon/android-chrome-512x512.png') %>
  <%= favicon_link_tag asset_path('favicon/Favicon_light@72x.png') %>
  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <script src="https://kit.fontawesome.com/30fa2950fc.js" crossorigin="anonymous"></script>
</head>

<body>
<%= turbo_frame_tag "flash" do %>
  <%= render "shared/flash" %>
<% end %>
<div class="home-link-container">
  <a href="/"><i class="fa-solid fa-house"></i></a>
</div>

<%= yield %>
<%= render "shared/dialog" %>
</body>
</html>
